import 'package:flutter/material.dart';

class SupportChatDetailPage extends StatelessWidget {
  const SupportChatDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
