import 'package:flutter/material.dart';

class Dealspage extends StatelessWidget {
  const Dealspage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Deals Page',style: TextStyle(color: Colors.white),),
      ),
    );
  }
}
