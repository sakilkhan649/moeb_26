import 'package:flutter/material.dart';

class Ridespage extends StatelessWidget {
  const Ridespage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rides Page',style: TextStyle(color: Colors.white),),
      ),
    );
  }
}
